{
  "name": "llama-camera-app",
  "version": "1.0.0",
  "description": "Camera feed application with AI analysis",
  "scripts": {
    "dev": "cd client && npm start",
    "server": "cd server && python app.py",
    "test-server": "python test_pipeline.py",
    "dev:full": "concurrently \"npm run server\" \"npm run test-server\" \"npm run dev\"",
    "build": "cd client && npm run build",
    "test": "cd client && npm test",
    "install:all": "cd client && npm install && cd ../server && pip install -r requirements.txt"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 